# Quarkdown Android

This module packages a lightweight native Android editor and preview surface for Quarkdown-flavored writing.

Build a debug APK with:

```bash
./gradlew :quarkdown-android:assembleDebug
```

The GitHub workflow `.github/workflows/android-debug-apk.yml` builds the same target and uploads the debug APK as a workflow artifact.
